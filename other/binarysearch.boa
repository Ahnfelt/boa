#public binarySearch(array Array[String], value String) Int:
  #mutable low = 0, high = array.size
  while{low < high}:
    mid = (low + high) / 2
    if(array.at(mid) < value):
      low := mid + 1
    else:
      high := mid
  low

