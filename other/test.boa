#public binarySearch[T](alist Array[T], item T) T:
  #mutable first = 0, last = alist.size - 1, found = Bool.false

  while{first<=last and: !found}:
    midpoint = (first + last) // 2
    if(alist.at(midpoint) == item):
      found := Bool.true
    else:
      r =
        if(item < alist.at(midpoint)):
          last := midpoint - 1
        else:
          first := midpoint + 1

  found
